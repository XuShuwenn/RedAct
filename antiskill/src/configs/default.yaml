# Trajectory Rewriter Configuration

rewriter:
  # Token budget for truncating user messages.
  user_truncate_tokens: 500

logging:
  level: "INFO"
  format: "%(asctime)s - %(levelname)s - %(message)s"
